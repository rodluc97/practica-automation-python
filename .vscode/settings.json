{
    // 1. Formatter
    "editor.defaultFormatter": "ms-python.black-formatter",

     "python.terminal.activateEnvironment": true,
    
    // Habilita que el código se formatee (limpie) automáticamente al guardar el archivo.
    "editor.formatOnSave": true,
    // ===============================================
    // 2. Configuración del Intérprete y Entornos Virtuales
    // ===============================================
    
    // Define la ruta del intérprete de Python para este proyecto.
    "python.defaultInterpreterPath": ".venv/Scripts/python.exe",

    // Fuerza el uso de Pylance como lenguaje.
    "python.languageServer": "Pylance",
    
    // ===============================================
    // 3. Ocultar Archivos y Carpetas Innecesarias
    // ===============================================
    
    // Hace que estas carpetas no se muestren en el explorador de archivos de VS Code,
    // ya que solo contienen archivos generados que no necesitas editar directamente.
    "files.exclude": {
        "**/.venv": true,
        "**/__pycache__": true,
        "**/.git": true
    },
    "python.testing.pytestArgs": [
        "."
    ],
    "python.testing.unittestEnabled": false,
    "python.testing.pytestEnabled": true


}